---
name: typescript
description: 'Typescript for sistemAntrianQRcode. 18 conventions, 25 fixes.'
domain: typescript
triggers:
  - glob: '**/*.ts'
  - glob: '**/*.tsx'
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **93 real patterns** in **sistemAntrianQRcode**. This skill is auto-routed to agents when working on typescript files.

## 🔧 Problem Playbooks

### Fixed null crash in Date — prevents null/undefined runtime crashes

- export async function createQueue(

* export async function createQueue(adminId: string, counterId: string, name?: string) {

- adminId: string,

* const expiresAt = new Date(Date.now() + 24 _ 60 _ 60 \* 1000) // 24 hours

- counterId: string,

*

- name?: string

* const results = await db

- ) {

*     .insert(queues)

- const expiresAt = new Date(Date.now() + 24 _ 60 _ 60 \* 1000) // 24 hour

**Actionable Steps:**

1. Modified 1 files
2. identifier: Date
3. identifier: Antrian
4. identifier: ACTIVE
5. identifier: Get

### Fixed null crash in Hook — prevents null/undefined runtime crashes

- 'use client';

* 'use client'

-

*

- import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';

* import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'

- import { getQueueEntries, callNextEntry, removeQueueEntry } from '@/lib/queue-entries';

* import { getQueueEntries, callNextEntry, removeQueueEntry } from '@/lib/queue-entries'

-

*

- /\*\*

* /\*\*

**Actionable Steps:**

1. Modified 1 files
2. identifier: Hook
3. identifier: Poll
4. identifier: Consider
5. identifier: Error

### problem-fix in route.ts

-

*

-         { error: error.issues.map(e => e.message).join(', ') },

*         { error: error.issues.map((e) => e.message).join(', ') },

-     return NextResponse.json(

*     return NextResponse.json({ error: 'Gagal memindahkan pelanggan' }, { status: 500 })

-       { error: 'Gagal memindahkan pelanggan' },

* }

-       { status: 500 }

* }

-     )

*

- }
- }
-

📌 IDE AST Context: Modifie

**Actionable Steps:**

1. Modified 1 files

### problem-fix in route.ts

-     return NextResponse.json(

*     return NextResponse.json({ error: 'Gagal membuat akun. Silakan coba lagi.' }, { status: 500 })

-       { error: 'Gagal membuat akun. Silakan coba lagi.' },

* }

-       { status: 500 }

* }

-     )

*

- }
- }
-

📌 IDE AST Context: Modified symbols likely include [POST]

**Actionable Steps:**

1. Modified 1 files

### Fixed null crash in NextResponse — prevents null/undefined runtime crashes

-

*

-

*

-       {

*       {

-         counter: newCounter

*         counter: newCounter,

-         { error: error.issues.map(e => e.message).join(', ') },

*         { error: error.issues.map((e) => e.message).join(', ') },

-

*

-     return NextResponse.json(

*     return NextResponse.json({ error: 'Gagal membuat counter' }, { status: 500 })

-       { error: 'Gagal membuat counter'

**Actionable Steps:**

1. Modified 1 files
2. identifier: NextResponse
3. identifier: Gagal
4. identifier: PUT
5. identifier: Update

### Fixed null crash in Create — prevents null/undefined runtime crashes

- 'use server';

* 'use server'

-

*

- import { auth } from '@/lib/auth';

* import { auth } from '@/lib/auth'

- import { createQueue, deleteQueue, getAdminQueues } from '@/lib/queue';

* import { createQueue, deleteQueue, getAdminQueues } from '@/lib/queue'

- import { revalidatePath } from 'next/cache';

* import { revalidatePath } from 'next/cache'

-

*

- /\*\*

* /\*\*

- - Create a new queue a

**Actionable Steps:**

1. Modified 1 files
2. identifier: Create
3. identifier: FormData
4. identifier: Unauthorized
5. identifier: Queue

### problem-fix in route.ts

File updated (external): src/app/api/queue/[queueId]/stats/route.ts

Content summary (17 lines):
import { NextRequest, NextResponse } from 'next/server'
import { getQueueStats } from '@/lib/services/queue-stats'

export async function GET(
request: NextRequest,
{ params }: { params: Promise<{ queueId: string }> }
) {
try {
const { queueId } = await params
const stats = await getQueue

**Actionable Steps:**

1. Modified 1 files

### problem-fix in route.ts

File updated (external): src/app/api/queue/entries/[entryId]/mark-served/route.ts

Content summary (59 lines):
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/lib/auth'
import { db } from '@/lib/db'
import { queueEntries } from '@/lib/db/schema'
import { eq } from 'drizzle-orm'

/\*\*

- POST /api/queue/entries/:entryId/mark-served - Mark customer as served
  \*/
  export

**Actionable Steps:**

1. Modified 1 files

### problem-fix in queue.test.ts

-         expect(result.error.errors[0].path).toContain('firstName')

*         expect(result.error.issues[0].path).toContain('firstName')

-         expect(result.error.errors[0].message).toContain('less than 50 characters')

*         expect(result.error.issues[0].message).toContain('less than 50 characters')

-         expect(result.error.errors[0].message).toContain('only contain letters')

*

**Actionable Steps:**

1. Modified 1 files

### Fixed null crash in SELECT

- import postgres from 'postgres'

* import postgres from 'postgres'

-

*

- // Try direct connection (port 5432)

* // Try direct connection (port 5432)

- const directUrl = 'postgresql://postgres:9tmvdY1IaM91YMcK@db.lzfrvgplqwhmgtzkvima.supabase.co:5432/postgres'

* const directUrl =

-

* 'postgresql://postgres:9tmvdY1IaM91YMcK@db.lzfrvgplqwhmgtzkvima.supabase.co:5432/postgres'

- // Try poole

**Actionable Steps:**

1. Modified 1 files
2. identifier: Try
3. identifier: SELECT
4. identifier: Connected
5. identifier: JSON

### Fixed null crash in Database — uses a proper password hashing algorithm

- /\*\*

* /\*\*

- - Database seed script — creates initial super admin account

* - Database seed script — creates initial super admin account

- -

* -

- - Usage:

* - Usage:

- - npx tsx scripts/seed.ts

* - npx tsx scripts/seed.ts

- - # atau

* - # atau

- - npx ts-node scripts/seed.ts

* - npx ts-node scripts/seed.ts

- -

* -

- - Requires: DATABASE_URL dan SUPER_ADMIN_EMAI

**Actionable Steps:**

1. Modified 1 files
2. identifier: Database
3. identifier: Usage
4. identifier: Requires
5. identifier: Required

### Fixed null crash in SELECT

- import postgres from 'postgres'

* import postgres from 'postgres'

-

*

- // Try direct connection (port 5432)

* // Try direct connection (port 5432)

- const directUrl = 'postgresql://postgres:9tmvdY1IaM91YMcK@db.lzfrvgplqwhmgtzkvima.supabase.co:5432/postgres'

* const directUrl = 'postgresql://postgres:9tmvdY1IaM91YMcK@db.lzfrvgplqwhmgtzkvima.supabase.co:5432/postgres'

-

*

- // Try pooler

**Actionable Steps:**

1. Modified 1 files
2. identifier: Try
3. identifier: SELECT
4. identifier: Connected
5. identifier: JSON

### Fixed null crash in Required — uses a proper password hashing algorithm

- const connectionString = process.env.DATABASE_URL

* const connectionString =

-

* process.env.DATABASE_URL_DIRECT ||

- if (!connectionString) {

* process.env.DATABASE_URL?.replace(':6543', ':5432')

- console.error('❌ DATABASE_URL belum dikonfigurasi di .env.local')

*

- process.exit(1)

* if (!connectionString) {

- }

* console.error('❌ DATABASE_URL belum dikonfigurasi di .e

**Actionable Steps:**

1. Modified 1 files
2. identifier: Required
3. identifier: Supabase
4. identifier: Memulai
5. identifier: Get

### Fixed null crash in Menghubungkan — prevents null/undefined runtime crashes

- const connectionString = process.env.DATABASE_URL

* const connectionString =

-

* process.env.DATABASE_URL_DIRECT ||

- if (!connectionString) {

* process.env.DATABASE_URL?.replace(':6543', ':5432')

- console.error('❌ DATABASE_URL tidak ditemukan di .env.local')

*

- process.exit(1)

* if (!connectionString) {

- }

* console.error('❌ DATABASE_URL tidak ditemukan di .env.local

**Actionable Steps:**

1. Modified 1 files
2. identifier: Menghubungkan
3. identifier: Menjalankan
4. identifier: Migrasi
5. identifier: Skema

### Fixed null crash in Database — uses a proper password hashing algorithm

- /\*\*

* /\*\*

- - Database seed script — creates initial super admin account

* - Database seed script — creates initial super admin account

- -

* -

- - Usage:

* - Usage:

- - npx tsx scripts/seed.ts

* - npx tsx scripts/seed.ts

- - # atau

* - # atau

- - npx ts-node scripts/seed.ts

* - npx ts-node scripts/seed.ts

- -

* -

- - Requires: DATABASE_URL dan SUPER_ADMIN_EMAI

**Actionable Steps:**

1. Modified 1 files
2. identifier: Database
3. identifier: Usage
4. identifier: Requires
5. identifier: Memulai

### Fixed null crash in Database — prevents null/undefined runtime crashes

- /\*\*

* /\*\*

- - Database migration runner using Drizzle ORM

* - Database migration runner using Drizzle ORM

- -

* -

- - Usage:

* - Usage:

- - npx dotenv-cli -e .env.local -- npx tsx scripts/migrate.ts

* - npx dotenv-cli -e .env.local -- npx tsx scripts/migrate.ts

- - # or add to package.json scripts

* - # or add to package.json scripts

- -

* -

- - Requires: DATABASE_URL

**Actionable Steps:**

1. Modified 1 files
2. identifier: Database
3. identifier: Drizzle
4. identifier: ORM
5. identifier: Usage

### Fixed null crash in SELECT

- const sql = postgres('postgresql://postgres.lzfrvgplqwhmgtzkvima:9tmvdY1IaM91YMcK@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres', { max: 1 })

* // Try direct connection (port 5432)

-

* const directUrl = 'postgresql://postgres:9tmvdY1IaM91YMcK@db.lzfrvgplqwhmgtzkvima.supabase.co:5432/postgres'

- async function test() {

*

- try {

* // Try pooler connection (port 6543)

-     const resu

**Actionable Steps:**

1. Modified 1 files
2. identifier: Try
3. identifier: SELECT
4. identifier: Connected
5. identifier: JSON

### Fixed null crash in Required — uses a proper password hashing algorithm

- const connectionString = process.env.DATABASE_URL_DIRECT || process.env.DATABASE_URL

* const connectionString =

-

* process.env.DATABASE_URL_DIRECT ||

- if (!connectionString) {

* process.env.DATABASE_URL?.replace(':6543', ':5432')

- console.error('❌ DATABASE_URL belum dikonfigurasi di .env.local')

*

- process.exit(1)

* if (!connectionString) {

- }

* console.error('❌ DATABASE_URL b

**Actionable Steps:**

1. Modified 1 files
2. identifier: Required
3. identifier: Supabase
4. identifier: Memulai
5. identifier: Get

### problem-fix in fixtures.ts

File updated (external): e2e/fixtures.ts

Content summary (14 lines):
import { test as base } from '@playwright/test'

/\*\*

- Extended test fixtures for Queue Automation E2E tests
  \*/

export type Fixtures = object

export const test = base.extend<Fixtures>({
// Add fixture implementations here
})

export { expect } from '@playwright/test'

**Actionable Steps:**

1. Modified 1 files

### problem-fix in route.ts

File updated (external): src/app/api/notifications/stats/route.ts

Content summary (31 lines):
import { NextResponse } from 'next/server'
import { db } from '@/lib/db'
import { notifications } from '@/lib/db/schema'
import { eq, count } from 'drizzle-orm'

/\*\*

- GET /api/notifications/stats
- Get notification statistics
  \*/
  export async function GET() {
  try {
  const [totalResult, sentResult

**Actionable Steps:**

1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions

- 📐 **Fixed null crash in NextResponse — prevents null/undefined runtime crashes — confirmed 13x** — - import { NextResponse } from "next/server"

* import { NextResponse } from 'next/server'

- import
- 📐 **Updated vitest database schema — confirmed 4x** — - import { describe, it, expect } from 'vitest'

* import { describe, it, expect } from 'vitest'

- i
- 📐 **Strengthened types ClassValue — externalizes configuration for environment fl...** — - import { createId } from '@para

... [Truncated — see individual observations for full content]
