---
name: typescript-react
description: 'Typescript React for sistemAntrianQRcode. 1 gotchas, 4 conventions, 14 fixes.'
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: '**/*.tsx'
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **65 real patterns** in **sistemAntrianQRcode**. This skill is auto-routed to agents when working on typescript-react files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

### ❌ gotcha in theme-provider.tsx

File updated (external): src/components/theme-provider.tsx

Content summary (26 lines):
'use client'

import \* as React from 'react'
import { ThemeProvider as NextThemesProvider } from 'next-themes'

// next-themes renders an inline <script> to preve

- Modified 1 files

## 🔧 Problem Playbooks

### Fixed null crash in Button — formalizes the data contract with explicit types

- 'use client';

* 'use client'

-

*

- import { useState } from 'react';

* import { useState } from 'react'

- import { Button } from '@/components/ui/button';

* import { Button } from '@/components/ui/button'

- import {

* import {

- Dialog,

* Dialog,

- DialogContent,

* DialogContent,

- DialogHeader,

* DialogHeader,

- DialogTitle,

* DialogTitle,

- DialogTrigger,

* Dial

**Actionable Steps:**

1. Modified 1 files
2. identifier: Button
3. identifier: Dialog
4. identifier: DialogContent
5. identifier: DialogHeader

### Fixed null crash in QueueEntryCard — prevents null/undefined runtime crashes

- 'use client';

* 'use client'

-

*

- import { useQueueEntries, useCallNextMutation, useRemoveEntryMutation } from '@/hooks/use-queue';

* import { useQueueEntries, useCallNextMutation, useRemoveEntryMutation } from '@/hooks/use-queue'

- import { QueueEntryCard } from './queue-entry-card';

* import { QueueEntryCard } from './queue-entry-card'

- import { Button } from '@/components/ui/button';

**Actionable Steps:**

1. Modified 1 files
2. identifier: QueueEntryCard
3. identifier: Button
4. identifier: Card
5. identifier: CardContent

### Fixed null crash in Image — formalizes the data contract with explicit types

- 'use client'

* 'use client'

-

*

- import { useEffect, useState } from 'react'

* import { useEffect, useState } from 'react'

- import { toDataURL } from 'qrcode'

* import { toDataURL } from 'qrcode'

- import Image from 'next/image'

* import Image from 'next/image'

-

*

- interface QRCodeDisplayProps {

* interface QRCodeDisplayProps {

- value: string

* value: string

- size?: numbe

**Actionable Steps:**

1. Modified 1 files
2. identifier: Image
3. identifier: QRCodeDisplayProps
4. identifier: QRCodeDisplay
5. identifier: FFFFFF

### Fixed null crash in Switch

- 'use client';

* 'use client'

- import { useState } from 'react';

* import { useState } from 'react'

- import { Switch } from '@/components/ui/switch';

* import { Switch } from '@/components/ui/switch'

- import { Label } from '@/components/ui/label';

* import { Label } from '@/components/ui/label'

- import { Bell } from 'lucide-react';

* import { Bell } from 'lucide-react'

- entryId: string;

* **Actionable Steps:**

1. Modified 1 files
2. identifier: Switch
3. identifier: Label
4. identifier: Bell
5. identifier: NotificationPreferences

### Fixed null crash in Button

- 'use client';

* 'use client'

-

*

- import { useState } from 'react';

* import { useState } from 'react'

- import { Button } from '@/components/ui/button';

* import { Button } from '@/components/ui/button'

- import {

* import {

- Dialog,

* Dialog,

- DialogContent,

* DialogContent,

- DialogHeader,

* DialogHeader,

- DialogTitle,

* DialogTitle,

- DialogTrigger,

* Dial

**Actionable Steps:**

1. Modified 1 files
2. identifier: Button
3. identifier: Dialog
4. identifier: DialogContent
5. identifier: DialogHeader

### Fixed null crash in Card — improves module reusability

- 'use client';

* 'use client'

-

*

- import { useQuery } from '@tanstack/react-query';

* import { useQuery } from '@tanstack/react-query'

- import { useParams } from 'next/navigation';

* import { useParams } from 'next/navigation'

- import { Card, CardContent } from '@/components/ui/card';

* import { Card, CardContent } from '@/components/ui/card'

- import { Skeleton } from '@/components/ui

**Actionable Steps:**

1. Modified 1 files
2. identifier: Card
3. identifier: CardContent
4. identifier: Skeleton
5. identifier: Users

### problem-fix in notification-panel.tsx

File updated (external): src/components/admin/notification-panel.tsx

Content summary (77 lines):
'use client'

import { useQuery } from '@tanstack/react-query'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { RefreshCw, CheckCircle, XCircle, Clock } from 'lucide-react'
import { retryFailedNotificationsAction }

**Actionable Steps:**

1. Modified 1 files

### problem-fix in queue-join-form.tsx

File updated (external): src/app/q/[counterId]/queue-join-form.tsx

Content summary (148 lines):
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/lab

**Actionable Steps:**

1. Modified 1 files

### Fixed null crash in SignupInput — prevents null/undefined runtime crashes

-

* const [successMessage, setSuccessMessage] = useState('')

- const {

*

-     register,

* const {

-     handleSubmit,

*     register,

-     formState: { errors, isSubmitting },

*     handleSubmit,

- } = useForm<SignupInput>({

*     formState: { errors, isSubmitting },

-     resolver: zodResolver(signupSchema),

* } = useForm<SignupInput>({

- })

*     resolver: zodResolver(signupSchem

**Actionable Steps:**

1. Modified 1 files
2. identifier: SignupInput
3. identifier: POST
4. identifier: Content
5. identifier: Type

### Fixed null crash in Gagal — prevents null/undefined runtime crashes

-         }),

*           confirm[REDACTED]

-       })

*         }),

-

*       })

-       const result = await response.json()

*

-

*       const result = await response.json()

-       if (!response.ok) {

*

-         setServerError(result.error || 'Gagal membuat akun. Silakan coba lagi.')

*       if (!response.ok) {

-         return

*         setServerError(result.error || 'Gagal membuat akun.

**Actionable Steps:**

1. Modified 1 files
2. identifier: Gagal
3. identifier: Silakan
4. identifier: Auto
5. identifier: Akun

### problem-fix in page.tsx

File updated (external): src/app/admin/counters/create/page.tsx

Content summary (174 lines):
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'

**Actionable Steps:**

1. Modified 1 files

### problem-fix in cron-trigger.tsx

-     } catch (_error) {

*     } catch {

-     } catch (_error) {

*     } catch {

📌 IDE AST Context: Modified symbols likely include [CronTrigger]

**Actionable Steps:**

1. Modified 1 files

### problem-fix in page.tsx

File updated (external): src/app/queue/[queueId]/view/page.tsx

Content summary (85 lines):
'use client';

import { useQuery } from '@tanstack/react-query';
import { useParams } from 'next/navigation';
import { Card, CardContent } from '@/components/ui/card';
import { Skeleton } from '@/components/ui/skeleton';
import { Users } from 'lucide-react';

export default function QueueViewPage()

**Actionable Steps:**

1. Modified 1 files

### Fixed null crash in DELETE — prevents null/undefined runtime crashes

-     mutationFn: () => callNextEntry(initialQueue.id),

*     mutationFn: () => callNextEntryApi(initialQueue.id),

-     mutationFn: (entryId: string) => removeQueueEntry(entryId),

*     mutationFn: async (entryId: string) => {

-     onMutate: (entryId) => setRemovingId(entryId),

*       const res = await fetch(`/api/queue/entries/${entryId}`, { method: 'DELETE' })

-     onSuccess: () => {

* **Actionable Steps:**

1. Modified 1 files
2. identifier: DELETE
3. identifier: Error
4. identifier: Failed
5. identifier: Remove

## 📐 Conventions & Best Practices

### Project Conventions

- 📐 **discovery in page.tsx — confirmed 3x** — File updated (external): src/app/q/[counterId]/page.tsx

Content summary (38 lines):
import { notFou

- 📐 **what-changed in queue-list.tsx — confirmed 3x** — - phone: entry.phoneNumber,

*               phone: entry.phoneNumber || 'N/A',

📌 I

- 📐 **what-changed in page.tsx — confirmed 3x** — - {queues.slice(0, 5).map((queue: { id: string; name: string; entries: any[] }) => (

- 📐 **Strengthened types page** — - {queues.slice(0, 5).map((queue: any) => (

*               {queues.slice(0, 5).map((

## 🤔 Decisions & Trade-offs

- **trade-off in theme-toggle.tsx** — File updated (external): src/components/theme-toggle.tsx

Content summary (37 lines):
'use client'

- **decision in query-provider.tsx** — File updated (external): src/components/providers/query-provider.tsx

Content summary (21 lines):
'u

- **trade-off in create-queue-button.tsx** — File updated (external): src/components/queue/create-queue-button.tsx

Content summary (93 lines):
'

- **trade-off in cron-trigger.tsx** — File updated (external): src/components/admin/cron-trigger.tsx

Content summary (79 lines):
'use cli

- **trade-off in page.tsx** — File updated (external): src/app/page.tsx

Content summary (124 lines):
import { auth } from "@/lib/

---

_Auto-generated by BrainSync 🧠 | 65 patterns | 2026-04-03_
