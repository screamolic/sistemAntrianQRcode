# Phase 1.1: Initialize Next.js 15 Project

**Phase:** 1
**Plan:** 1.1
**Status:** Ready to Execute

---

## Objective

Set up Next.js 15 project with App Router, TypeScript, and base configuration.

---

## Tasks

### 1. Backup and Clean Old Files
- [ ] Create backup of old `pages/`, `components/`, `lib/` directories (move to `legacy/` folder)
- [ ] Remove old `package.json` and `package-lock.json`
- [ ] Remove old `next.config.js`
- [ ] Keep `.gitignore`, `README.md`, `.eslintrc.json` (will update later)

### 2. Initialize Next.js 15
- [ ] Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`
- [ ] Verify installation completed successfully
- [ ] Check `package.json` has Next.js 15, React 19, TypeScript 5+

### 3. Verify Development Server
- [ ] Run `npm run dev`
- [ ] Verify app loads at http://localhost:3000
- [ ] Check no TypeScript errors in console

### 4. Update Git Ignore
- [ ] Ensure `.planning/` is in `.gitignore`
- [ ] Ensure `.env`, `.env.local` are ignored
- [ ] Ensure `node_modules/` is ignored

---

## Verification Criteria

- [ ] Next.js 15 installed (check `package.json`)
- [ ] `src/app/` directory exists
- [ ] `tsconfig.json` has strict mode enabled
- [ ] `npm run dev` starts without errors
- [ ] Default page renders at localhost:3000
- [ ] TypeScript path alias `@/*` works

---

## Notes

- Use `--force` flag if create-next-app complains about existing files
- Old code will be in `legacy/` folder for reference during migration
- Don't modify base Next.js config yet - wait for shadcn/ui installation

---

**Estimated Time:** 15 minutes
**Dependencies:** None
**Artifacts:** Fresh Next.js 15 project structure
