# Plan 3.2: QR Code Generation Component

**Phase:** 3 - Queue Core Functionality  
**Goal:** Implement QR code display component with download functionality  
**Dependencies:** Plan 3.1 complete  

---

## Implementation Steps

### 1. Install QR Code Library

```bash
npm install qrcode.react
```

### 2. Create QR Code Display Component

**File:** `src/components/queue/qr-code-display.tsx`

```typescript
'use client';

import { useRef } from 'react';
import { QRCodeSVG } from 'qrcode.react';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';

interface QRCodeDisplayProps {
  value: string; // Queue URL
  size?: number;
  includeDownload?: boolean;
}

export function QRCodeDisplay({ 
  value, 
  size = 256, 
  includeDownload = true 
}: QRCodeDisplayProps) {
  const svgRef = useRef<SVGSVGElement>(null);

  const handleDownload = () => {
    if (!svgRef.current) return;
    
    const svgData = new XMLSerializer().serializeToString(svgRef.current);
    const canvas = document.createElement('canvas');
    const ctx = canvas.getContext('2d');
    const img = new Image();
    
    img.onload = () => {
      canvas.width = size;
      canvas.height = size;
      ctx?.drawImage(img, 0, 0);
      const pngFile = canvas.toDataURL('image/png');
      
      const downloadLink = document.createElement('a');
      downloadLink.download = `queue-qr-${Date.now()}.png`;
      downloadLink.href = pngFile;
      downloadLink.click();
    };
    
    img.src = 'data:image/svg+xml;base64,' + btoa(svgData);
  };

  return (
    <Card className="w-fit">
      <CardContent className="pt-6 space-y-4">
        <QRCodeSVG
          ref={svgRef}
          value={value}
          size={size}
          level="M"
          includeMargin={true}
          className="mx-auto"
        />
        {includeDownload && (
          <Button onClick={handleDownload} className="w-full">
            Download QR Code
          </Button>
        )}
      </CardContent>
    </Card>
  );
}
```

### 3. Create Queue Share Dialog

**File:** `src/components/queue/queue-share-dialog.tsx`

```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from '@/components/ui/dialog';
import { QRCodeDisplay } from './qr-code-display';
import { Share } from 'lucide-react';

interface QueueShareDialogProps {
  queueId: string;
  queueUrl: string;
}

export function QueueShareDialog({ queueId, queueUrl }: QueueShareDialogProps) {
  const [open, setOpen] = useState(false);

  const handleCopyLink = async () => {
    await navigator.clipboard.writeText(queueUrl);
  };

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button variant="outline" size="icon">
          <Share className="h-4 w-4" />
        </Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Share Queue</DialogTitle>
        </DialogHeader>
        <div className="space-y-4">
          <QRCodeDisplay value={queueUrl} size={200} />
          <div className="flex gap-2">
            <Input value={queueUrl} readOnly className="flex-1" />
            <Button onClick={handleCopyLink} variant="outline">
              Copy
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

### 4. Add QR Code to Queue Page

**File:** `src/app/queue/[queueId]/page.tsx` (create in Plan 3.4)

```typescript
import { QRCodeDisplay } from '@/components/queue/qr-code-display';
import { formatQueueUrl } from '@/lib/utils';

export default async function QueuePage({ params }: { params: { queueId: string } }) {
  const queue = await getQueue(params.queueId);
  const queueUrl = formatQueueUrl(params.queueId);

  return (
    <div className="container mx-auto py-8">
      <h1 className="text-2xl font-bold mb-4">{queue?.name}</h1>
      <QRCodeDisplay value={queueUrl} size={256} />
    </div>
  );
}
```

---

## Must-Haves

- [ ] `qrcode.react` installed
- [ ] QRCodeDisplay component with SVG rendering
- [ ] Download functionality working
- [ ] QueueShareDialog component
- [ ] Copy link to clipboard feature
- [ ] QR code displays queue URL
- [ ] Size configurable (default 256x256)

---

## Verification Criteria

1. **Visual Testing:**
   - QR code renders correctly
   - Scans to correct URL with phone camera
   - Download produces valid PNG file

2. **Functional Testing:**
   - Download button works
   - Copy link copies to clipboard
   - Dialog opens/closes correctly

3. **Accessibility:**
   - Alt text on QR code
   - Keyboard navigation in dialog
   - Screen reader compatible

---

## Dependencies

- **Plan 3.1:** Queue generation
- **Packages:** `qrcode.react`, `lucide-react` (for icons)

---

## Estimated Effort

**Time:** 1-2 hours  
**Complexity:** Low  

---

*Created: 31 March 2026 — Plan 3.2 for Queue Core Functionality*
